// *********************** PROGRAMMERS ONLY ************************
function solution(arr) {
  const result = arr.map((item) => item * 2);

  return result;
}

const arr = [1, 2];

console.log(solution(arr));
